// Same reference as the generated (git-ignored) expo-env.d.ts, so typecheck also works in CI
// where `expo start` never runs. Provides types for CSS modules, assets, etc.
/// <reference types="expo/types" />
